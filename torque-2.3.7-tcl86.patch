diff -p -up torque-2.3.7/src/cmds/qstat.c.tcl86~ torque-2.3.7/src/cmds/qstat.c
--- torque-2.3.7/src/cmds/qstat.c.tcl86~	2009-07-22 19:19:36.827824470 -0400
+++ torque-2.3.7/src/cmds/qstat.c	2009-07-22 19:34:26.309847405 -0400
@@ -1830,7 +1830,7 @@ tcl_init(void)
   if (Tcl_Init(interp) == TCL_ERROR)
     {
     fprintf(stderr, "Tcl_Init error: %s",
-            interp->result);
+	    Tcl_GetStringResult(interp));
     }
 
 #if TCLX
@@ -1843,7 +1843,7 @@ tcl_init(void)
     {
 #endif
     fprintf(stderr, "Tclx_Init error: %s",
-            interp->result);
+            Tcl_GetStringResult(interp));
     }
 
 #endif /* TCLX */
@@ -1954,10 +1954,10 @@ void tcl_run(
     trace = (char *)Tcl_GetVar(interp, "errorInfo", 0);
 
     if (trace == NULL)
-      trace = interp->result;
+	trace = (char*)Tcl_GetStringResult(interp);
 
     fprintf(stderr, "%s: TCL error @ line %d: %s\n",
-            script, interp->errorLine, trace);
+            script, Tcl_GetErrorLine(interp), trace);
     }
 
   Tcl_DeleteInterp(interp);
diff -p -up torque-2.3.7/src/scheduler.tcl/pbs_tclWrap.c.tcl86~ torque-2.3.7/src/scheduler.tcl/pbs_tclWrap.c
--- torque-2.3.7/src/scheduler.tcl/pbs_tclWrap.c.tcl86~	2009-07-22 19:22:30.128069919 -0400
+++ torque-2.3.7/src/scheduler.tcl/pbs_tclWrap.c	2009-07-22 19:23:11.176903070 -0400
@@ -76,6 +76,8 @@
 * This license will be governed by the laws of the Commonwealth of Virginia,
 * without reference to its choice of law rules.
 */
+#define USE_INTERP_RESULT
+
 #include <pbs_config.h>   /* the master config generated by configure */
 
 #include <stdio.h>
